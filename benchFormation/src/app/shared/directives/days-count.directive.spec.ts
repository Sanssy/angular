import { DaysCountDirective } from './days-count.directive';

describe('DaysCountDirective', () => {
  it('should create an instance', () => {
    const directive = new DaysCountDirective();
    expect(directive).toBeTruthy();
  });
});
